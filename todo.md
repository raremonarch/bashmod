# bashmod todo

1. If module installed and update available, there should be an 'update' button in addition to the 'uninstall' button.
1. add a filter for only showing `local` modules and `remote` modules, similar to the category filter.
